/**
 * getAverageAge - A function that calculates the average age from an array of objects.
 *
 * @param {Array<{ name: string, age: number }>} array - An array of objects, each object having `name` and `age` properties
 * @returns {number} - The average age
 *
 * Usage:
 * const array = [{ name: "John", age: 20 }, { name: "Jane", age: 22 }];
 * const result = getAverageAge(array);
 * Output: 21
 */
function getAverageAge(array) {
}

module.exports = getAverageAge;
