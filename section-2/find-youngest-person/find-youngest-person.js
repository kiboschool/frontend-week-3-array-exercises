/**
 * findYoungestPerson - A function that finds the youngest person from an array of objects.
 *
 * @param {Array<{ name: string, age: number }>} array - An array of objects, each object having `name` and `age` properties
 * @returns {string} - The name of the youngest person
 *
 * Usage:
 * const array = [{ name: "John", age: 20 }, { name: "Jane", age: 19 }];
 * const result = findYoungestPerson(array);
 * Output: "Jane"
 */
function findYoungestPerson(array) {
}

module.exports = findYoungestPerson;
