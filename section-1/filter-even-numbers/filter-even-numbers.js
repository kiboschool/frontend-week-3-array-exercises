/**
 * filterEvenNumbers - A function that filters out odd numbers from the array.
 *
 * @param {Array<number>} numArray - An array of numbers
 * @returns {Array<number>} - An array containing only even numbers
 *
 * Usage:
 * const numArray = [1, 2, 3, 4, 5];
 * const result = filterEvenNumbers(numArray);
 * Output: [2, 4]
 */
function filterEvenNumbers(numArray) {
}

module.exports = filterEvenNumbers;
