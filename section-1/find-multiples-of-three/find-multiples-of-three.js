/**
 * findMultiplesOfThree - A function that finds the first number in the array that is a multiple of three.
 *
 * @param {Array<number>} numArray - An array of numbers
 * @returns {number} - The first number in the array that is a multiple of three
 *
 * Usage:
 * const numArray = [1, 2, 3, 4, 5];
 * const result = findMultiplesOfThree(numArray);
 * Output: 3
 */
function findMultiplesOfThree(numArray) {
}

module.exports = findMultiplesOfThree;
