/**
 * findPositiveNumbers - A function that returns an array with all the positive numbers.
 *
 * @param {Array<number>} numArray - An array of numbers
 * @returns {Array<number>} - An array with all the positive numbers
 *
 * Usage:
 * const numArray = [-1, 2, -3, 4, -5];
 * const result = findPositiveNumbers(numArray);
 * Output: [2, 4]
 */
function findPositiveNumbers(numArray) {
}

module.exports = findPositiveNumbers;
